#!/bin/bash

if [ $# -ne 1 ]
then
    echo "no brightness specified!" >&2
    exit 1
fi


displays=$(xrandr -q | grep " connected" | cut -d ' ' -f1 | while read dis; do echo -n "--output $dis "; done)

xrandr $displays --brightness $( echo $1 / 100 | bc -l ) 2>/dev/null

exit 0

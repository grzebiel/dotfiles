#!/bin/bash

modified()
{
    [[ ! `$1 rev-parse HEAD` = `$1 rev-parse origin/master` ]]  || [[ ! `$1 status --porcelain` = "" ]]
}

modified config && c=C
modified wiki && w=W

[[ ! -z $w ]]  || [[ ! -z $c ]] && echo [$c$w]
